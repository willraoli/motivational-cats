---
const res = await fetch(
  `
    ${import.meta.env.PUBLIC_CAT_API_URL}/images/search?api_key=
    ${import.meta.env.CAT_API_KEY}
  `
)
const cat = await res.json().catch(err => console.log(err))
---

<img
  src={cat[0]?.url}
  class='rounded-full sepia shadow-md'
  width='350'
/>
